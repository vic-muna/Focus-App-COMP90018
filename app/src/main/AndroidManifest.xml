<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        NOTE (Permissions - intentionally left commented out):
        The mechanisms described in the project plan (location zones, shake/
        face-down gestures, ambient volume detection, app restriction, and
        cloud sync) will each need a runtime permission below. They are
        commented out for now because this skeleton does not access any of
        them yet - uncomment the ones you need as you implement each feature,
        and remember Android also requires you to *request* dangerous
        permissions at runtime, not just declare them here.

        <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
        <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
        <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
        <uses-permission android:name="android.permission.RECORD_AUDIO" />
        <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />
        <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
        <uses-permission android:name="android.permission.INTERNET" />
        <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    -->

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:supportsRtl="true">

        <!--
            Single-Activity app: every screen is a Composable, navigated
            between with Jetpack Navigation Compose (see
            ui/navigation/NavGraph.kt). No other Activities are needed.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
